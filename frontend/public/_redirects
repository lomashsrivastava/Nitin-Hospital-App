# Netlify Redirects for Nitin Hospital App
# This file is copied directly to the build output folder (dist)

# 1. Proxy all API calls to the Render backend (force redirect with 200!)
/api/*  https://nitin-hospital-backend.onrender.com/api/:splat  200!

# 2. Redirect all other requests to index.html for React Router (SPA fallback)
/*      /index.html                                            200
